{
  "name": "gh-runners-auto-scale",
  "version": "0.0.7",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "deno run --allow-net --allow-read --allow-env --allow-run src/index.ts",
    "dev": "deno run --allow-net --allow-read --allow-env --allow-run --watch src/index.ts",
    "fmt": "deno fmt src",
    "lint": "deno lint src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "smee-client": "^1.2.3"
  }
}